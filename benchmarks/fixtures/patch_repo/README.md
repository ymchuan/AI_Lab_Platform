# Patch Fixture Project

This fixture is used only by `benchmarks/patch_task_eval.py`.

## Hardware

The project has one main RTX 5090 node and one planned mixed-GPU node.

## Benchmark

The benchmark suite checks model latency, RAG oracle answers, and agent planning.
