# 网络配置

## 网络拓扑

```text
┌─────────────────────────────────────────────────────────────────┐
│ 校园网 / 内网 NAT                                                 │
│                                                                 │
│  5090 主机             新设备                                    │
│  172.16.14.240         172.16.x.x                                │
│  RTX 5090 32GB         RTX 4090D 24GB + RTX 4060 Ti 16GB          │
│  qwen/qwen3.6-27b 已部署          尚未接入                         │
│                                                                 │
│  出口 NAT: 123.127.159.1                                        │
└───────────────┬────────────────────┬────────────────────┘
                │                    │
                │ SSH :12340         │ SSH :12341
                │ 手动开启           │ 待接入
                ▼                    ▼
┌──────────────────────────────────────────────────────────────────┐
│ 腾讯云 CVM：82.156.69.153                                        │
│ Ubuntu 24.04 · 2核 · 2GB                                         │
│                                                                  │
│ LiteLLM :8000 -> 当前路由 qwen-local 到 5090                     │
│ 规划：HTTPS 入口 + 多节点模型路由                                  │
│ 约束：不再把 OpenWebUI / RAG / Agent Runtime 常驻在云服务器       │
└──────────────────────────────────────────────────────────────────┘
```

## 端口使用

| 端口 | 位置 | 服务 | 访问范围 | 状态 |
|------|------|------|---------|------|
| 1234 | 5090 | LM Studio API | 本机/局域网 | ✅ 已用 |
| 12340 | 云服务器 | SSH 隧道 -> 5090 | 仅本机 | ⏸️ 需手动开启 |
| 12341 | 云服务器 | SSH 隧道 -> 新设备 | 仅本机 | ⏳ 待接入 |
| 8000 | 云服务器 | LiteLLM API | 公网 | ✅ 已用 |
| 3000 | 云服务器或本地节点 | OpenWebUI | 需要时开放 | ⚠️ 不建议云端常驻 |
| 22 | 云服务器 | SSH Server | 公网 | ✅ 已用 |

## NAT 限制

内网设备都在校园网 NAT 后面：

```text
5090:      172.16.14.240
新设备:     172.16.x.x
出口 NAT:   123.127.159.1
```

8060S 当前无法使用，暂不纳入网络拓扑和端口规划。

新设备的 4090D 24GB + 4060 Ti 16GB 可以按资源池理解为 40GB 总显存，但不是单卡连续显存。网络和路由规划应把它当作两张独立 GPU 看待。

限制：

- 外部无法直接访问内网设备。
- 没有公网 IP 和端口映射权限。
- 实验室限制下不优先使用 FRP / Tailscale / ZeroTier。

解决方案：每台需要对外提供服务的设备主动建立 SSH Reverse Tunnel 到云服务器。当前只有 5090 需要手动开启 `:12340` 隧道。

## SSH Reverse Tunnel

命令格式：

```bash
ssh -N -R <云服务器端口>:127.0.0.1:<本机端口> ubuntu@82.156.69.153
```

建议参数：

```text
ServerAliveInterval=30    每 30 秒发一次心跳
ServerAliveCountMax=10    10 次无回应才断开
ExitOnForwardFailure=yes  端口绑定失败时退出
```

云服务器端保活（`sshd_config`）：

```text
ClientAliveInterval 30
ClientAliveCountMax 10
```

## 腾讯云安全组

入站规则：

```text
TCP 22    - SSH
TCP 8000  - LiteLLM API Gateway
TCP 3000  - OpenWebUI（仅需要时开放，优先迁移到本地节点）
```

安全建议：

- API Key 不写入公开文档。
- 如果 8000 暴露公网，优先限制来源 IP。
- 后续引入 Nginx 或 Caddy 做 HTTPS、访问日志和限流。

## 已知问题

### NAT 回环

内网设备可能无法通过云服务器公网 IP 访问自己。

解决：5090 本机直接连接 `http://127.0.0.1:1234/v1`，外部客户端连接云服务器公网 Base URL。

### 服务器内存限制

2GB 内存不能稳定承载 OpenWebUI、RAG、Agent Runtime 等重服务。

当前策略：云服务器只做轻量入口；OpenWebUI、RAG、Agent Runtime、向量库等迁移到本地节点。

